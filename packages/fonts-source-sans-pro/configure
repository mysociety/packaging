#!/bin/sh

[ -f ../../.venv/packaging/bin/activate ] && source ../../.venv/packaging/bin/activate

./bin/configure.py

if [ ! -d fonts-source-sans-pro ] ; then
    tar xzf fonts-source-sans-pro.tgz
fi
